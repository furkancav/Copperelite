# Web UI + AI listing pipeline
Flask==3.1.3
gunicorn==23.0.0
requests==2.32.5
python-dotenv==1.2.1
PyYAML==6.0.3

# Görsel işleme (AVIF/WEBP/HEIC → JPEG, sips yerine — Linux uyumlu)
Pillow==11.3.0
pillow-avif-plugin==1.5.5
pillow-heif==1.1.1

# Scraper (opsiyonel — CLI için)
beautifulsoup4==4.15.0
lxml>=5.0
